# AI聚合工具

一个兼容OpenAI API的AI模型聚合代理，支持多个AI供应商的统一接口。

## ✨ 功能特性

- ✅ 完全兼容OpenAI API格式
- ✅ 支持多个AI供应商统一接入
- ✅ 流式和非流式响应支持
- ✅ 模型名称格式：`供应商/模型`
- ✅ 自动获取供应商模型列表
- ✅ 支持手动配置模型列表
- ✅ 配置文件热重载
- ✅ Token验证机制
- ✅ 健康检查和监控
- ✅ 统一错误处理
- ✅ 异步请求支持
- ✅ 可配置日志级别
- ✅ 连接池管理
- ✅ 响应大小限制
- ✅ 性能监控

## 🚀 快速开始

### 1. 安装依赖

```bash
pip install -r requirements.txt
```

### 2. 配置服务

复制配置示例文件：
```bash
cp config.json.example config.json
```

编辑 `config.json`：
```json
{
  "host": "localhost",
  "port": 8001,
  "log_level": "WARNING",
  "providers": [
    {
      "provider": "zhipu",
      "baseurl": "https://open.bigmodel.cn/api/paas/v4",
      "token": "your-api-key",
      "model_list": []
    },
    {
      "provider": "openai",
      "baseurl": "https://api.openai.com/v1",
      "token": "sk-your-key",
      "model_list": ["gpt-4", "gpt-3.5-turbo"]
    }
  ],
  "tokens": [
    "my-token|sk-test-123456"
  ],
  "supported_models": [
    ".*"
  ]
}
```

### 3. 启动服务

```bash
python api.py
```

服务将在配置的端口启动（默认8001）。

## 📋 配置说明

### config.json 配置项

| 配置项 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| `host` | string | 否 | `localhost` | 服务监听地址 |
| `port` | int | 否 | `8080` | 服务监听端口 |
| `log_level` | string | 否 | `WARNING` | 日志级别（DEBUG/INFO/WARNING/ERROR/CRITICAL） |
| `providers` | array | 是 | `[]` | 供应商配置列表 |
| `tokens` | array | 是 | `[]` | API Token白名单 |
| `supported_models` | array | 否 | `[]` | 支持的模型正则表达式 |
| `max_connections` | int | 否 | `100` | 最大连接数 |
| `max_keepalive_connections` | int | 否 | `20` | 最大保持连接数 |
| `keepalive_expiry` | float | 否 | `30.0` | 连接过期时间（秒） |
| `stream_timeout` | float | 否 | `300.0` | 流式请求超时（秒） |
| `non_stream_timeout` | float | 否 | `30.0` | 非流式请求超时（秒） |
| `max_response_size` | int | 否 | `10485760` | 最大响应大小（字节，默认10MB） |

### 供应商配置格式

**新格式（推荐）**：使用JSON对象
```json
{
  "provider": "供应商名称",
  "baseurl": "API_URL",
  "token": "API_KEY",
  "model_list": ["model1", "model2"]
}
```

**旧格式（兼容）**：使用字符串
```
供应商名称|API_URL|API_KEY
```

#### 配置说明

- `provider`: 供应商名称，用于模型ID前缀（如 `zhipu/glm-4`）
- `baseurl`: API基础URL，会自动去除末尾的斜杠
- `token`: API密钥或访问令牌
- `model_list`: 可选，模型列表
  - 如果为空数组 `[]`，或者不填，则自动从供应商API获取模型列表
  - 如果指定模型，则只使用指定的模型，不会自动获取

#### 示例

```json
"providers": [
  {
    "provider": "zhipu",
    "baseurl": "https://open.bigmodel.cn/api/paas/v4",
    "token": "your-zhipu-key",
  },
  {
    "provider": "openai",
    "baseurl": "https://api.openai.com/v1",
    "token": "sk-your-openai-key",
    "model_list": ["gpt-4", "gpt-3.5-turbo", "gpt-4-turbo"]
  },
  {
    "provider": "anthropic",
    "baseurl": "https://api.anthropic.com/v1",
    "token": "sk-ant-your-key",
    "model_list": []
  }
]
```

### Token配置格式

```
描述|token值
```

示例：
```json
"tokens": [
  "admin|sk-admin-123456",
  "user1|sk-user1-789012"
]
```

### 完整配置示例

```json
{
  "host": "localhost",
  "port": 8001,
  "log_level": "WARNING",
  
  "providers": [
    {
      "provider": "zhipu",
      "baseurl": "https://open.bigmodel.cn/api/paas/v4",
      "token": "your-api-key",
      "model_list": []
    },
    {
      "provider": "openai",
      "baseurl": "https://api.openai.com/v1",
      "token": "sk-your-key",
      "model_list": ["gpt-4", "gpt-3.5-turbo"]
    }
  ],
  
  "tokens": [
    "admin|sk-admin-123456",
    "user1|sk-user1-789012"
  ],
  
  "supported_models": [
    ".*glm.*",
    ".*gpt.*",
    ".*claude.*"
  ],
  
  "max_connections": 100,
  "max_keepalive_connections": 20,
  "keepalive_expiry": 30.0,
  "stream_timeout": 300.0,
  "non_stream_timeout": 30.0,
  "max_response_size": 10485760
}
```

### 配置说明

#### 日志级别 (log_level)

| 级别 | 说明 | 适用场景 |
|------|------|---------|
| `DEBUG` | 详细调试信息 | 开发环境 |
| `INFO` | 一般信息 | 测试环境 |
| `WARNING` | 警告（默认） | 生产环境 |
| `ERROR` | 错误 | 高性能生产环境 |
| `CRITICAL` | 严重错误 | 仅记录严重问题 |

#### 连接池配置

- `max_connections`: 最大连接数，控制并发请求上限
- `max_keepalive_connections`: 保持活跃的连接数，提高复用效率
- `keepalive_expiry`: 连接保持时间，超时后自动关闭

#### 超时配置

- `stream_timeout`: 流式请求超时时间，建议300秒以上
- `non_stream_timeout`: 非流式请求超时时间，通常30秒足够

#### 响应大小限制

- `max_response_size`: 最大响应大小（字节），防止内存耗尽
  - 默认10MB (10485760字节)
  - 可根据实际需求调整

#### 模型列表配置

供应商的 `model_list` 字段有两种使用方式：

1. **自动获取模式**（推荐）
   ```json
   {
     "provider": "openai",
     "baseurl": "https://api.openai.com/v1",
     "token": "sk-your-key"
   }
   ```
   - 设置为空数组 `[]`
   - 程序会自动调用供应商的 `/models` 接口获取模型列表
   - 适用于模型列表经常变化的供应商

2. **手动指定模式**
   ```json
   {
     "provider": "openai",
     "baseurl": "https://api.openai.com/v1",
     "token": "sk-your-key",
     "model_list": ["gpt-4", "gpt-3.5-turbo", "gpt-4-turbo"]
   }
   ```
   - 明确指定可用的模型列表
   - 程序不会调用供应商API获取模型
   - 适用于：
     - 供应商不提供模型列表接口
     - 只想暴露部分模型
     - 减少API调用次数

## 🔌 API端点

### 基础端点

- `GET /` - 服务信息和可用端点
- `GET /health` - 健康检查
- `POST /v1/reload` - 重新加载配置（热重载）

### OpenAI兼容端点

- `GET /v1/models` - 获取所有可用模型
- `POST /v1/chat/completions` - 聊天补全（支持流式）

## 📖 使用示例

### 1. 健康检查

```bash
curl http://localhost:8001/health
```

响应：
```json
{
  "status": "healthy",
  "providers": {
    "zhipu": true,
    "openai": true
  },
  "healthy_providers": 2,
  "total_providers": 2
}
```

### 2. 获取模型列表

```bash
curl http://localhost:8001/v1/models
```

响应：
```json
{
  "object": "list",
  "data": [
    {
      "id": "zhipu/glm-4",
      "object": "model",
      "owned_by": "zhipu"
    },
    {
      "id": "openai/gpt-4",
      "object": "model",
      "owned_by": "openai"
    }
  ]
}
```

### 3. 非流式聊天补全

```bash
curl -X POST http://localhost:8001/v1/chat/completions \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer sk-test-123456" \
  -d '{
    "model": "zhipu/glm-4",
    "messages": [
      {"role": "user", "content": "你好，请介绍一下自己"}
    ],
    "temperature": 0.7,
    "max_tokens": 100
  }'
```

### 4. 流式聊天补全

```bash
curl -X POST http://localhost:8001/v1/chat/completions \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer sk-test-123456" \
  -d '{
    "model": "zhipu/glm-4",
    "messages": [
      {"role": "user", "content": "写一首诗"}
    ],
    "stream": true
  }'
```

### 5. 热重载配置

```bash
curl -X POST http://localhost:8001/v1/reload
```

响应：
```json
{
  "message": "配置重新加载成功",
  "providers_count": 2
}
```

## 🔒 Token验证

所有 `/v1/chat/completions` 请求都需要提供有效的Bearer Token。

### 使用Token

```bash
curl -X POST http://localhost:8001/v1/chat/completions \
  -H "Authorization: Bearer sk-test-123456" \
  -H "Content-Type: application/json" \
  -d '{...}'
```

### 无Token访问

如果未提供Token或Token无效，将返回401错误：
```json
{
  "error": {
    "message": "未提供API token",
    "type": "http_error",
    "code": 401
  }
}
```

## 🎯 高级特性

### 1. 流式响应优化

- ✅ 三层防护机制确保连接关闭
- ✅ 处理客户端断开（CancelledError）
- ✅ 超时优化（流式300秒，非流式30秒）
- ✅ 数据块计数监控

### 2. 非流式响应优化

- ✅ 区分流式/非流式请求方式
- ✅ 响应大小限制（10MB）
- ✅ 性能监控（耗时+大小）
- ✅ 优化的读取方式

### 3. 连接池管理

- ✅ 最大连接数：100
- ✅ 保持连接数：20
- ✅ 连接过期时间：30秒

### 4. 健康检查

- ✅ 并发检查所有供应商
- ✅ 返回详细的健康状态
- ✅ 支持定期监控

### 5. 灵活的模型管理

- ✅ 支持自动获取模型列表
- ✅ 支持手动指定模型列表
- ✅ 支持正则表达式过滤模型
- ✅ 兼容新旧配置格式

## 📊 性能特性

所有性能参数均可通过 `config.json` 配置：

| 特性 | 默认值 | 配置项 | 说明 |
|------|--------|--------|------|
| 流式超时 | 300秒 | `stream_timeout` | 流式请求的最大等待时间 |
| 非流式超时 | 30秒 | `non_stream_timeout` | 非流式请求的最大等待时间 |
| 最大响应大小 | 10MB | `max_response_size` | 单个响应的最大字节数 |
| 最大连接数 | 100 | `max_connections` | 连接池的最大连接数 |
| 保持连接数 | 20 | `max_keepalive_connections` | 保持活跃的连接数 |
| 连接过期时间 | 30秒 | `keepalive_expiry` | 空闲连接的过期时间 |
| 并发健康检查 | 是 | - | 并发检查所有供应商健康状态 |

## 🛠️ 运维管理

### 日志管理

日志文件：`ai_proxy.log`

查看实时日志：
```bash
tail -f ai_proxy.log
```

查看错误日志：
```bash
grep ERROR ai_proxy.log
```

查看性能统计：
```bash
grep "耗时:" ai_proxy.log | awk '{print $(NF-1)}' | sort -n
```

### 配置热重载

修改 `config.json` 后，无需重启服务：
```bash
curl -X POST http://localhost:8001/v1/reload
```

支持热重载的配置项：
- ✅ 供应商列表
- ✅ Token白名单
- ✅ 支持的模型
- ✅ 日志级别
- ✅ 模型列表配置

### 监控指标

程序会记录以下监控信息：
- 请求耗时
- 响应大小
- 连接状态
- 健康检查结果
- 错误统计

## 🐛 故障排查

### 问题1: 连接超时

**症状**: 请求超时或连接失败

**解决方法**:
1. 检查供应商API是否可访问
2. 验证API Key是否正确
3. 查看日志中的错误信息
4. 使用 `/health` 端点检查供应商状态

### 问题2: Token验证失败

**症状**: 返回401错误

**解决方法**:
1. 确认Token在 `config.json` 的 `tokens` 列表中
2. 检查Token格式是否正确
3. 确认请求头中包含 `Authorization: Bearer `

### 问题3: 模型不可用

**症状**: 返回"未找到模型"错误

**解决方法**:
1. 使用 `/v1/models` 查看可用模型列表
2. 确认模型名称格式：`供应商/模型`
3. 检查 `supported_models` 配置
4. 如果使用手动指定模式，确认模型在 `model_list` 中

### 问题4: 日志过多

**症状**: 日志文件过大

**解决方法**:
1. 调整日志级别为 `WARNING` 或 `ERROR`
2. 配置日志轮转（logrotate）
3. 定期清理旧日志

### 问题5: 模型列表为空

**症状**: `/v1/models` 返回空列表

**解决方法**:
1. 检查供应商的 `model_list` 配置
   - 如果为空数组，确认供应商API可访问
   - 如果手动指定，确认模型名称正确
2. 查看日志中的错误信息
3. 使用 `/health` 检查供应商状态
4. 尝试手动指定模型列表

## 🔧 性能优化说明

### 流式传输优化

1. **三层防护机制**
   - 生成器内部的 try-except-finally
   - API层的异常处理
   - 最终的 finally 清理块

2. **资源管理**
   - 所有连接确保关闭
   - 处理客户端断开（CancelledError）
   - 数据块计数监控

3. **超时优化**
   - 流式请求支持长时间响应（默认300秒）
   - 可根据模型特性调整

### 非流式传输优化

1. **请求方式优化**
   - 流式请求使用 stream=True
   - 非流式请求直接POST（更高效）

2. **响应大小保护**
   - 检查 Content-Length 头
   - 验证实际读取大小
   - 双重保护机制

3. **性能监控**
   - 记录请求耗时
   - 记录响应大小
   - 便于性能分析

### 连接池管理

1. **连接复用**
   - 保持活跃连接减少握手开销
   - 自动清理过期连接

2. **并发控制**
   - 限制最大连接数防止资源耗尽
   - 支持高并发场景

### 调优建议

#### 高并发场景
```json
{
  "max_connections": 200,
  "max_keepalive_connections": 50,
  "stream_timeout": 600.0
}
```

#### 低延迟场景
```json
{
  "max_connections": 50,
  "max_keepalive_connections": 10,
  "non_stream_timeout": 10.0
}
```

#### 大模型场景
```json
{
  "stream_timeout": 600.0,
  "max_response_size": 52428800
}
```

## 🔄 配置迁移指南

### 从旧格式迁移到新格式

**旧格式**:
```json
{
  "providers": [
    "zhipu|https://open.bigmodel.cn/api/paas/v4|your-key",
    "openai|https://api.openai.com/v1|sk-your-key"
  ]
}
```

**新格式**:
```json
{
  "providers": [
    {
      "provider": "zhipu",
      "baseurl": "https://open.bigmodel.cn/api/paas/v4",
      "token": "your-key"
    },
    {
      "provider": "openai",
      "baseurl": "https://api.openai.com/v1",
      "token": "sk-your-key"
    }
  ]
}
```

**注意**: 程序同时支持新旧两种格式，可以混合使用。

## 📄 许可证

MIT License